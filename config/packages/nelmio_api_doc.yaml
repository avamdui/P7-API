nelmio_api_doc:
    models: { use_jms: false }
    documentation:
        servers:
          - url: https://127.0.0.1:8000/
            description: API over HTTP
        info:
            title: BileMo 
            description: Téléphones mobiles haut de gamme.
            version: 1.0.0
            
        tags:
            - name: "Customer"
              description: " Get details about Customer"
            - name: "Phones"
              description: "Get details and list of phones"
            - name: "Client"
              description: "Get details about Client"
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        default:
            disable_default_routes: true


        
